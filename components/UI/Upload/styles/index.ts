import { Upload } from 'antd'
import styled from 'styled-components'

export const UploaderUiStyled = styled(Upload)`
  .ant-upload-list {
    display: flex;
    justify-content: start;
    flex-direction: row-reverse;
  }
`
