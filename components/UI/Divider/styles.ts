import { Divider } from 'antd'
import styled from 'styled-components'

export const DividerContainer = styled(Divider)`
  margin: 32px 0;
  border-block-start: 1px solid ${props => props.theme.colors.border}30;
`
